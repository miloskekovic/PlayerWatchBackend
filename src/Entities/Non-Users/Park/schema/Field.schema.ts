const FieldSchema = {
    name: String,
    description: String,
    has_seating: Boolean,
    has_shade: Boolean,
    is_turf: Boolean,
    pictures: [String],
    electricity: Boolean,
    sports: [String],
    is_indoor: Boolean,
    spikes: Boolean,
    size: String,
    highschool_compatible: Boolean,
}
export default FieldSchema
